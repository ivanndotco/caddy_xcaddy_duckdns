# DuckDNS Credentials
# Required for DNS-01 ACME challenge with DuckDNS
#
# How to get your token:
# 1. Log in to DuckDNS (https://www.duckdns.org/)
# 2. Your token is displayed at the top of the page
# 3. Copy the token value

DUCKDNS_API_TOKEN=your_duckdns_token_here

# Domain Configuration
# Note: DuckDNS domains are in format: yourdomain.duckdns.org
DOMAIN=yourdomain.duckdns.org

# WordPress Configuration (if using WordPress example)
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=your_secure_password_here
DB_ROOT_PASSWORD=your_root_password_here

# Grafana Configuration (if using monitoring example)
GRAFANA_USER=admin
GRAFANA_PASSWORD=your_grafana_password_here

# Notes:
# - Never commit this file with real credentials to git
# - Copy this file to .env and fill in your real values
# - The .env file is already in .gitignore
# - DuckDNS provides free subdomain names (e.g., myapp.duckdns.org)
# - Your token can be found at: https://www.duckdns.org/
